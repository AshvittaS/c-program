#include<stdio.h>
struct lnode{
    int val;
   struct lnode *next;   
};
typedef struct lnode node;
node * head=NULL;
void insert (int val){
    node *newn;
    if(head==NULL){
        newn->val;
        newn->next=NULL;
        head
    }
}